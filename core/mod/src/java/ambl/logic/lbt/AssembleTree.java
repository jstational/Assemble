package ambl.logic.lbt;

import arc.scene.ui.*;
import arc.struct.*;

// Essentially, a file tree for logic blocks with folders, when i implement this system,
// i need to include metadata when compiling
// for now, the AssembleTree will be unused
public class AssembleTree extends ScrollPane {
    public AssembleTreeItem rootTreeItem;

    public AssembleTree() {
        this.rootTreeItem = new A
    }
}