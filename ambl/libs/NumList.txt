unless LVar is reworked and lists are finally added, the NumList library requires a link to a MemoryBlock

==================================

NumList [Group Object]

.length(): Number

.nonZeroItems(): Number

.put(n, i): Void

.remove(i): Number

.clear(): Void

.putAll(List other): VOid

for getting a item here, do numlist[i]

==================================

NumList
.interp
       .serp(NumList x, NumList y, Number n): Number - nearest neighbor
       .lperp(NumList x, NumList y, Number n): Number - linear piecewise
       .cserp(NumList x, NumList y, Number n): Number - cubic spline interpolation
       .crerp(NumList x, Number t): Number - catmull rom interpolation
       .lrerp(NumList x, NumList y, Number n): Number - lagrange interpolation